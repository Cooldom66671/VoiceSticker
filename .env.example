# НЕ КОММИТЬТЕ .env ФАЙЛ В GIT!

# Telegram Bot Configuration
BOT_TOKEN=148908811:AAETbzOhSmuUwW58G_aGrCrJrQD0e8u1QxE
# Model Configuration (optional)
WHISPER_MODEL=base              # tiny, base, small, medium, large
STABLE_DIFFUSION_MODEL=runwayml/stable-diffusion-v1-5

# Performance Settings (optional)
WHISPER_DEVICE=cpu              # cpu, cuda, mps
SD_DEVICE=mps                   # cpu, cuda, mps
SD_DTYPE=float16                # float16, float32
SD_NUM_INFERENCE_STEPS=30       # 20-50, меньше = быстрее
SD_GUIDANCE_SCALE=7.5           # 5-15, больше = точнее следует промпту

# Storage Settings (optional)
STORAGE_DIR=storage
LOGS_DIR=logs
DB_PATH=stickers.db

# Timeouts (optional, в секундах)
WHISPER_TIMEOUT=30
SD_TIMEOUT=60
DB_TIMEOUT=5

# Logging (optional)
LOG_LEVEL=INFO                  # DEBUG, INFO, WARNING, ERROR